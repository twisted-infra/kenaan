This is source material and a Dockerfile for building a Docker image to run kenaan, twistedmatrix.com's irc bot.

The source code for the bot is installed in `/srv/kenaan/`.
The bot is configured to log to `/var/log/ircbot.log` and run in `/var/run/ircbot/`.
